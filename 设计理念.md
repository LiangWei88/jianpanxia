# 设计理念: 为什么做一个全局 Vim 风格导航层

## 问题: 我们在键盘上浪费了多少时间？

先想一下你每天在电脑上做的动作:

- 打字时双手放在主行 (home row)
- 需要移动光标 → 右手离开主行,移动到方向键区 (或鼠标)
- 需要翻页 → 手去找 PageUp/PageDown
- 需要浏览器后退 → 去点地址栏左边的后退按钮,或者按 `Alt + ←` (左手要离开主行)
- 需要删一行 → 去按 Delete 键
- 需要快速跳到文档开头 → 去按 `Ctrl + Home`

每次手离开主行,都会产生三个成本:

1. **时间成本** — 从主行到方向键再回来,大约 0.3-0.5 秒。一天重复几百次就是几分钟。
2. **定位成本** — 眼睛要离开屏幕去找键位。
3. **注意力成本** — 你从"输入/阅读"的状态切到了"找键"的状态,再切回来需要重新聚焦。

> 键盘效率的核心矛盾不是"按键少",而是 **手尽量不离开主行**。

---

## 灵感来源

### Vim: 模式化 (modal) 键盘哲学

Vim 发明了一种革命性的思路: **把键盘分成"模式"**。

- **Insert 模式**: 正常打字
- **Normal 模式**: `h j k l` 控制方向,`w b` 跳单词,`gg` / `G` 跳首尾,`x` 删除

好处是: 在 Normal 模式下,你做所有导航操作时,双手几乎完全留在主行。但 Vim 的局限也很明显 — 它只在 Vim 编辑器里有效。

### Vimium: 把 Vim 带进浏览器

[Vimium](https://vimium.github.io/) 是一款浏览器扩展,它让你用 `j k` 翻页、`J K` 前进后退、`f` 点击链接。

Vimium 证明了 **键盘驱动的导航不仅在编辑器里好用,在浏览器里也一样顺手**。但 Vimium 只存在于浏览器内部 — 你用 VS Code、Notepad、文件管理器、设置面板时,它帮不上忙。

### Home Row Mods: 把修饰键搬到主行

Home Row Mods (HRM) 是一种键盘固件技巧 — 把主行上的某个字母在"按住"时变成修饰键 (Ctrl / Alt / Shift),按下时还是字母本身。比如:

- `A` 单击 = a, 按住 = Ctrl
- `S` 单击 = s, 按住 = Shift
- `D` 单击 = d, 按住 = Alt

这样你可以 **不离开主行就按 Ctrl+C、Alt+Tab**。HRM 本质上是在用"tap vs hold"的区分来增加键的功能,代价是学习成本较高。

### 三者的共同点

| 技术 | 核心理念 |
|:--|:--|
| Vim | 模式切换 — 打字时正常,导航时专注 |
| Vimium | 键盘优先 — 导航不需要鼠标 |
| Home Row Mods | 主行最大化 — 手不离开主行 |

**把这三者的思想合成一个方案,就是本项目要做的事。**

---

## 本项目的设计思路

### 核心: CapsLock 作为全局模式切换键

为什么选 CapsLock?

- 位置很好 — 左手小拇指最容易按到的"废弃键"
- 默认没人用 — 几乎所有现代键盘工作流都用不到大写锁定
- tap / hold 天然分离 — 短按当 Esc, 按住激活 Nav Layer, 两个动作互不干扰

### 全局 Nav Layer

这是关键的差异化设计:

- **Vim 模式** 只在编辑器里生效
- **Vimium** 只在浏览器里生效
- **Home Row Mods** 是把修饰键藏在字母里,没有独立的导航层

本项目把 Vim 的导航操作做成一个 **操作系统级的全局 Layer**:

```
CapsLock 按住 → Nav Layer 激活 (所有应用中)
  ├── H J K L    → 方向键
  ├── W B        → 跳单词
  ├── U I        → 翻页
  ├── Y O        → 行首/行尾
  ├── G / Shift+G → 文档头/尾
  ├── ; / '       → 浏览器前进/后退
  ├── X          → Delete
  └── , / M      → 鼠标滚轮
```

在 VS Code 里管用,在浏览器里管用,在文件管理器里管用,在 Notepad 里也管用。

### 与 Home Row Mods 的关系

Home Row Mods 追求的是 **修饰键不离主行**,适合高频组合键 (Ctrl+C、Alt+Tab 等)。本项目追求的是 **导航不离主行**,适合方向移动、翻页、跳行等操作。

两者可以互补 — 你完全可以在使用本项目的同时,在其他层里加上 Home Row Mods 的思路。

---

## 具体好处

| 好处 | 说明 |
|:--|:--|
| **手不离主行** | 方向键、翻页、跳行全部用主行或上一行的按键完成,右手不需要往方向键区跑 |
| **全应用覆盖** | VS Code、浏览器、终端、文件管理器、设置面板 — 只要是能接收键盘输入的应用都生效 |
| **学习成本低** | HJKL、W/B、Y/O — 如果你用过 Vim 或 Vimium,大部分键位一看就懂;没用过也只有不到 20 个按键 |
| **不改变打字习惯** | Base Layer 保持原始行为,打字和正常按键完全不受影响。只有 CapsLock 被复用 |
| **Esc 的补偿** | CapsLock 短按 = Esc,Vim 用户和终端重度使用者可以直接获益 |
| **滚轮的惯性加速** | Caps+,/M 按住时模拟触控板的惯性滚动,比单步滚轮平滑得多 |

---

## 与常见方案的对比

| 方案 | 优点 | 缺点 |
|:--|:--|:--|
| **纯鼠标** | 可视化,所见即所得 | 手频繁切换,精度低,上下文切换成本高 |
| **Vim 模式 (editor only)** | HJKL 顺手 | 只在编辑器里有效,离开编辑器立刻失效 |
| **Vimium / Vimium C** | 浏览器内完美 | 仅限浏览器,非浏览器应用帮不上忙 |
| **PowerToys / AHK 单键映射** | 简单 | 无法做 tap/hold 区分,全局 layer 管理很麻烦 |
| **Home Row Mods** | 修饰键不离主行 | 学习成本高,tap/hold 误判是常见问题 |
| **本项目 (Kanata Nav Layer)** | 全局生效 + 手不离主行 + 学习曲线平缓 | 需要安装 Kanata;少数高权限应用可能需要以管理员运行 |

---

## 关于实现: 从 AHK v1 到 Kanata

这个方案最初是用 AutoHotkey v1 写的,但 AHK v1 在 2024 年已停止维护。迁移到 Kanata 的原因:

| AHK v1 | Kanata |
|:--|:--|
| 单文件脚本,启动慢 | 编译型,Rust 编写,极低延迟 |
| tap/hold 实现麻烦 (Timer 模拟) | 原生 `tap-hold` action,配置化 |
| 全局 layer 靠变量 + 热键组合实现 | 原生 layer 系统,语义清晰 |
| 滚轮加速需要自己写算法 | 原生 `mwheel-accel-up/down` |
| 官方已停止维护 | 活跃维护中,社区健康 |

Kanata 的配置是 S-expression (Lisp 风格),虽然第一次看起来有点怪,但好处是: **每个 action 都是一个清晰的函数调用**,没有脚本语言的副作用和隐式行为。修改配置后可以用 `--check` 直接验证语法,非常适合迭代开发。

---

## 致谢

这个项目站在很多人的肩膀上:

- **Bram Moolenaar** 创造了 Vim 的 modal 键盘哲学
- **Vimium 团队** 把 Vim 理念带到了浏览器
- **QMK 社区** 普及了 layer、tap-hold、home row mods 等键盘固件技巧
- **jtroo (Kanata 作者)** 写了一个让键盘配置真正优雅的工具
- **Interception / Microsoft** 提供了 Windows 键盘拦截的底层能力

如果你看到了这个项目,也欢迎去看看他们的工作。
